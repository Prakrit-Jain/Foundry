[profile.default]
src = "src"
out = "out"
libs = ["lib"]
[rpc_endpoints]
avalanche = "${AVALANCHE_RPC_URL}"

[etherscan]
avalanche = { key = "${ETHERSCAN_API_KEY}" }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config